{
  "name": "automator",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel --filter @automator/api --filter @automator/worker --filter @automator/desktop dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "test:coverage": "pnpm -r --if-present test:coverage",
    "coverage:modules": "pnpm run test:coverage && mkdir -p coverage && cargo llvm-cov --manifest-path apps/desktop/src-tauri/Cargo.toml --lcov --output-path coverage/rust.lcov && node scripts/check-module-function-coverage.mjs"
  },
  "engines": {
    "node": ">=20.11.0",
    "pnpm": ">=9.0.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4"
  }
}
